kind: pipeline
name: default

steps:
- name: docker  
  image: plugins/docker
  settings:
    username:
      from_secret: dockerhub_username
    password:
      from_secret: dockerhub_token
    repo: elit3ge/docker-baseimage-alpine
    tags:
    - latest
  environment:
    # Enable BuildKit for this step
    DOCKER_BUILDKIT: 1
